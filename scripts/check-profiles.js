require("dotenv").config(); const { createClient } = require("@supabase/supabase-js"); const supabase = createClient(process.env.SUPABASE_URL, process.env.SUPABASE_ANON_KEY); async function checkProfiles() { console.log("🔍 Verificando estrutura da tabela profiles...\n"); try { const { data, error } = await supabase .from("profiles") .select("*") .limit(1); if (error) { console.error("❌ Erro:", error.message); return; } console.log("📋 Estrutura da tabela profiles:"); if (data && data.length > 0) { console.log("Campos disponíveis:", Object.keys(data[0])); console.log("Exemplo:", data[0]); } else { console.log("Tabela vazia"); } } catch (error) { console.error("❌ Erro interno:", error.message); } } checkProfiles();
