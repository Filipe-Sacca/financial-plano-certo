require("dotenv").config(); const { createClient } = require("@supabase/supabase-js"); const supabase = createClient(process.env.SUPABASE_URL, process.env.SUPABASE_ANON_KEY); async function checkClients() { console.log("🔍 Verificando tabela clients...\n"); try { const { data: clients, error } = await supabase .from("clients") .select("*") .limit(5); if (error) { console.error("❌ Erro ao buscar clients:", error.message); return; } console.log("👥 Clients disponíveis:"); if (clients && clients.length > 0) { clients.forEach((c, i) => console.log(`${i+1}. id: ${c.id}, name: ${c.name}, email: ${c.email}`)); console.log(`\n🎯 Client_id dos produtos: f133bf28-ff34-47c3-827d-dd2b662f0363`); console.log("   Esse ID existe na tabela clients?", clients.some(c => c.id === "f133bf28-ff34-47c3-827d-dd2b662f0363") ? "✅ Sim" : "❌ Não"); } else { console.log("   Nenhum client encontrado"); } } catch (error) { console.error("❌ Erro interno:", error.message); } } checkClients();
