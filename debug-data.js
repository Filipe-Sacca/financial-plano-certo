require("dotenv").config(); const { createClient } = require("@supabase/supabase-js"); const supabase = createClient(process.env.SUPABASE_URL, process.env.SUPABASE_ANON_KEY); async function checkData() { console.log("🔍 Verificando dados relacionados...\n"); try { const { data: products } = await supabase.from("products").select("client_id, merchant_id").limit(3); console.log("📦 Products client_ids e merchant_ids:"); products.forEach((p, i) => console.log(`${i+1}. client_id: ${p.client_id}, merchant_id: ${p.merchant_id}`)); const { data: merchants } = await supabase.from("ifood_merchants").select("client_id, merchant_id, name").limit(5); console.log("\n🏪 iFood Merchants:"); if (merchants && merchants.length > 0) { merchants.forEach((m, i) => console.log(`${i+1}. client_id: ${m.client_id}, merchant_id: ${m.merchant_id}, name: ${m.name}`)); } else { console.log("   Nenhum merchant encontrado"); } } catch (error) { console.error("❌ Erro:", error.message); } } checkData();
