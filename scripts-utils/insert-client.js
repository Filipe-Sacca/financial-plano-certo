require("dotenv").config(); const { createClient } = require("@supabase/supabase-js"); const supabase = createClient(process.env.SUPABASE_URL, process.env.SUPABASE_ANON_KEY); async function insertClient() { console.log("👤 Criando cliente...\n"); try { const { data, error } = await supabase .from("clients") .insert([{ id: "f133bf28-ff34-47c3-827d-dd2b662f0363", name: "Cliente de Teste", email: "teste@email.com", created_at: new Date().toISOString() }]) .select(); if (error) { console.error("❌ Erro:", error.message); return; } console.log("✅ Cliente criado com sucesso\!"); console.log(data); } catch (error) { console.error("❌ Erro interno:", error.message); } } insertClient();
